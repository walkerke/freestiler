% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freestile.R
\name{freestile_layer}
\alias{freestile_layer}
\title{Create a layer specification with per-layer zoom range}
\usage{
freestile_layer(input, min_zoom = NULL, max_zoom = NULL)
}
\arguments{
\item{input}{An sf data frame.}

\item{min_zoom}{Integer. Minimum zoom level for this layer. If NULL, uses the
global min_zoom from \code{freestile()}.}

\item{max_zoom}{Integer. Maximum zoom level for this layer. If NULL, uses the
global max_zoom from \code{freestile()}.}
}
\value{
A freestile_layer object (list with class attribute).
}
\description{
Wraps an sf object with optional per-layer zoom range overrides for use in
multi-layer tile generation.
}
\examples{
\dontrun{
library(sf)
nc <- st_read(system.file("shape/nc.shp", package = "sf"))
roads <- st_read("roads.shp")

freestile(
  list(
    counties = freestile_layer(nc, min_zoom = 0, max_zoom = 10),
    roads = freestile_layer(roads, min_zoom = 8, max_zoom = 14)
  ),
  "layers.pmtiles"
)
}

}
