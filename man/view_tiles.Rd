% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view.R
\name{view_tiles}
\alias{view_tiles}
\title{Quickly view a PMTiles file on an interactive map}
\usage{
view_tiles(
  input,
  layer = NULL,
  layer_type = NULL,
  color = NULL,
  opacity = 0.5,
  port = 8080,
  promote_id = NULL
)
}
\arguments{
\item{input}{Path to a local \code{.pmtiles} file.}

\item{layer}{Character. Source layer name to display. If \code{NULL}
(default), the first layer in the tileset is used.}

\item{layer_type}{Character. Map layer type: \code{"fill"}, \code{"line"},
or \code{"circle"}. If \code{NULL}, auto-detected from the PMTiles
metadata geometry type.}

\item{color}{Fill, line, or circle color. Default is \code{"navy"} for fill
and line layers, \code{"steelblue"} for circle layers.}

\item{opacity}{Numeric opacity (0--1). Default is 0.5.}

\item{port}{Port for the local tile server. Default is 8080.}

\item{promote_id}{Character. Property name to use as the feature ID for
hover interactivity. If \code{NULL}, no feature promotion is used.}
}
\value{
A mapgl map object (can be piped into further mapgl operations).
}
\description{
Starts a local tile server (if needed) and creates an interactive mapgl map
showing the tileset. Layer type and styling are auto-detected from the
PMTiles metadata when possible.
}
\examples{
\dontrun{
freestile(nc, "nc.pmtiles", layer_name = "counties")
view_tiles("nc.pmtiles")

# Override auto-detection
view_tiles("roads.pmtiles", layer_type = "line", color = "red")

# Point data
view_tiles("airports.pmtiles", layer_type = "circle", color = "orange")
}

}
\seealso{
\code{\link[=serve_tiles]{serve_tiles()}}, \code{\link[=freestile]{freestile()}}
}
